.kubeconfig

# Go
/dist/
*.out

# Failure-diagnostics dump from `just capture-diagnostics` (may contain cluster
# logs/state; CI uploads it as an artifact instead of committing it).
/preview-diagnostics/

# nix build symlinks
result
result-*

# local, per-developer env loaded by the dev shell (secrets/config)
.env
.coverage
