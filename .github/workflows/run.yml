---
name: run
on:
  push:
    branches: [main, master]
  pull_request:
permissions: {}
env:
  FORCE_COLOR: ""
jobs:
  uvx:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - name: Install uv
        uses: astral-sh/setup-uv@d31148d669074a8d0a63714ba94f3201e7020bc3 # ratchet:astral-sh/setup-uv@v8.3.0
        with:
          enable-cache: true

      - name: Run via uvx from GitHub PR head
        env:
          REPO: ${{ github.event.pull_request.head.repo.full_name || github.repository }}
          REF: ${{ github.head_ref || github.ref_name }}
        run: uvx --refresh --from "git+https://github.com/${REPO}.git@${REF}" dantofa.cli --version
