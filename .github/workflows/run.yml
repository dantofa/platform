---
name: run
on:
  push:
    branches: [main, master]
  pull_request:
permissions: {}
env:
  FORCE_COLOR: ""
jobs:
  uvx:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - name: Install uv
        uses: astral-sh/setup-uv@f98e06938123ccabd21905ea5d0069192241f9f1 # ratchet:astral-sh/setup-uv@v8.3.1
        with:
          enable-cache: true

      - name: Run via uvx from GitHub PR head
        env:
          REPO: ${{ github.event.pull_request.head.repo.full_name || github.repository }}
          REF: ${{ github.head_ref || github.ref_name }}
        run: uvx --refresh --from "git+https://github.com/${REPO}.git@${REF}" dantofa.cli --version
